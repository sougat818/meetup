version: '2'
services:
    meetup-postgresql:
        image: postgres:9.5.4
        # volumes:
        #     - ~/volumes/jhipster/meetup/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=meetup
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
